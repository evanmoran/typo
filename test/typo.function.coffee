
typo = require '../src/typo.coffee'

describe 'typo.function', ->

  it 'success does not throw', ->
    # expect(typo 'should not fail', str, 'string').to.equal true

  it 'failure throws', ->
    # expect(typo 'should fail', str, 'integer').to.throw 'should fail'



